package com.meksconway.rickandmorty.common

import com.meksconway.rickandmorty.R

enum class BottomTabs(val navId: Int) {
    Characters(R.id.nav_character),
    Locations(R.id.nav_location),
    Episodes(R.id.nav_episode)
}